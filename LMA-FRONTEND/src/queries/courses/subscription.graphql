mutation createSubscriber($profile_Id: ID!, $module_enrolled: ID!) {
  createCourseModuleSubscriber(
    data: { user_profile: $profile_Id, module_enrolled: $module_enrolled }
  ) {
    data {
      id
      attributes {
        createdAt
      }
    }
  }
}
